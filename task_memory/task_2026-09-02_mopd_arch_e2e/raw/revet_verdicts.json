[
  {
    "claim_id": "C1",
    "claim": "mt_opd.py defines exactly 7 module-level functions at build_domain_weights:24, select_routed_teacher_logprobs:65, compute_domain_share_metrics:90, compute_domain_loss_weights:159, refresh_opd_advantage:338, apply_teacher_conflict_policy:405, compute_teacher_conflict_metrics:494; file is 562 lines; __all__ (14-21) omits refresh_opd_advantage.",
    "verdict": "CONFIRMED",
    "evidence": "/data/ycfeng/Open-MOPD/training/verl/verl/workers/actor/mt_opd.py \u2014 `grep -n \"^def \"` returns exactly: 24:def build_domain_weights(, 65:def select_routed_teacher_logprobs(, 90:def compute_domain_share_metrics(, 159:def compute_domain_loss_weights(, 338:def refresh_opd_advantage(, 405:def apply_teacher_conflict_policy(, 494:def compute_teacher_conflict_metrics(. No top-level `class`, `async def`, or decorator lines exist (`grep -n '^\\(class\\|async def\\|@\\)'` returns nothing). `grep -c \"\"` = 562 lines; last line 562 is `    return out`. __all__ spans lines 14-21 and lists: \"build_domain_weights\", \"select_routed_teacher_logprobs\", \"compute_domain_share_metrics\", \"compute_teacher_conflict_metrics\", \"compute_domain_loss_weights\", \"apply_teacher_conflict_policy\" \u2014 6 names, refresh_opd_advantage absent. Side note (does not affect the claim): the omission is inert because dp_actor.py:46 does `from verl.workers.actor.mt_opd import refresh_opd_advantage` explicitly (used at dp_actor.py:1100).",
    "correct_fact": "",
    "cluster": "mt_opd_kernels"
  },
  {
    "claim_id": "C2",
    "claim": "build_domain_weights: a domain label not in domain_order does NOT raise; it gets a uniform 1/N row (mt_opd.py:60-61).",
    "verdict": "CONFIRMED",
    "evidence": "mt_opd.py:56-62:\n56\t    for sample_idx, d in enumerate(domains):\n57\t        teacher_idx = domain_to_idx.get(str(d), -1)\n58\t        if teacher_idx >= 0:\n59\t            weights[sample_idx, teacher_idx] = 1.0\n60\t        else:\n61\t            weights[sample_idx] = 1.0 / n\n62\t    return weights\nThe lookup uses `.get(str(d), -1)` (line 57), so an unknown label yields -1 and falls to the `else` branch at 60-61 writing a full 1/N row. No raise on this path; the docstring at line 41-42 states the same (\"Samples whose domain is not in ``domain_order`` fall back to uniform\").",
    "correct_fact": "",
    "cluster": "mt_opd_kernels"
  },
  {
    "claim_id": "C3",
    "claim": "compute_domain_loss_weights returns None when target_shares is None (:207), and a domain absent from target_shares gets target 0.0 (:225), meaning zero gradient for its sequences.",
    "verdict": "CONFIRMED",
    "evidence": "mt_opd.py:207-208: `if target_shares is None or domains is None or len(domains) == 0:` / `        return None`. mt_opd.py:225: `raw = {d: float(target_shares.get(d, 0.0)) for d in present}` \u2014 `.get(d, 0.0)` gives an absent domain target 0.0. Propagation to zero gradient: line 229 `target = {d: raw[d] / tsum for d in present}` keeps it 0; line 288 `info[dom] = (idx, target[dom] / observed, mag)` stores scale 0.0; lines 328-330 `for dom, (idx, scale, _mag) in info.items(): for i in idx: weights[i] = scale` writes 0.0 to those sequences; the final rescale (334) `weights = weights * (total / denom)` is multiplicative so 0 stays 0. Edge case worth noting (does not contradict the claim): lines 226-227 `if sum(raw.values()) <= 0: raw = {d: 1.0 / len(present) for d in present}` \u2014 only when EVERY present domain is absent from target_shares does it fall back to an equal split. Separately, the docstring at lines 188-189 (\"Missing domains default to an equal split across those present\") overstates that fallback relative to the code.",
    "correct_fact": "",
    "cluster": "mt_opd_kernels"
  },
  {
    "claim_id": "C4",
    "claim": "compute_domain_loss_weights ends by rescaling so the token-weighted mean of the weights is 1 (:333-334).",
    "verdict": "CONFIRMED",
    "evidence": "mt_opd.py:332-335:\n332\t    # Token-weighted mean of 1: preserves total loss magnitude, shifts only shares.\n333\t    denom = (weights * per_seq).sum().clamp(min=1e-6)\n334\t    weights = weights * (total / denom)\n335\t    return weights\nWith `per_seq = mask.sum(dim=-1).float()` (line 219) and `total = per_seq.sum().clamp(min=1.0)` (line 220), after line 334 sum(w_i * n_i) == total == sum(n_i), i.e. the token-weighted mean of the weights equals 1. These are the last statements before the return.",
    "correct_fact": "",
    "cluster": "mt_opd_kernels"
  },
  {
    "claim_id": "C5",
    "claim": "reward_scale_direction=\"divide\" is documented in-source as measured-harmful (:290-296, citing IF weight 27 -> 91 and grad share 57% vs 33% target); \"multiply\" clamps to [0.05, 20] while \"divide\" has no clamp.",
    "verdict": "CONFIRMED",
    "evidence": "mt_opd.py:290-296 (comment block): `# M2 direction. \"divide\" (the original, measured harmful 2026-08-04/06): divide` / `# by current magnitude -- at rest this up-weights the quiet domain, but over` / `# training the easy domain's gap collapses ~35x while math/code collapse ~2x,` / `# so 1/mag**alpha snowballs onto the *easiest* domain (measured: IF loss` / `# weight 27 -> 91, grad token share 57% vs target 33%). \"multiply\" multiplies` / `# by the magnitude ratio instead, i.e. gradient budget follows the *remaining*` / `# teacher gap: a domain whose gap has collapsed loses share automatically.` Clamp asymmetry, mt_opd.py:316-325:\n316\t            if reward_scale_direction == \"divide\":\n319\t                if m <= 1e-8:\n320\t                    continue\n321\t                sc = sc / (m ** alpha)\n322\t            else:\n323\t                fac = (max(m, 1e-12) / max(ref[dom], 1e-12)) ** alpha\n324\t                fac = min(max(fac, 0.05), 20.0)\n325\t                sc = sc * fac\nThe divide branch only skips on a vanishing magnitude (<=1e-8); no bound on `sc`. Only the multiply branch clamps, to [0.05, 20.0] at line 324. (Also consistent: line 166 shows the parameter default is `reward_scale_direction: str = \"divide\"`, i.e. the harmful mode is the default.)",
    "correct_fact": "",
    "cluster": "mt_opd_kernels"
  },
  {
    "claim_id": "C6",
    "claim": "apply_teacher_conflict_policy with policy=\"none\" returns the SAME routed tensor object (identity, not a copy) at :440-441.",
    "verdict": "CONFIRMED",
    "evidence": "mt_opd.py:438-441:\n438\t    if policy not in {\"none\", \"mask\", \"consensus\"}:\n439\t        raise ValueError(f\"unknown teacher conflict policy: {policy!r}\")\n440\t    if policy == \"none\" or teacher_logprobs is None or len(teacher_logprobs) < 2:\n441\t        return routed_logprobs, None, {}\nLine 441 returns the input parameter `routed_logprobs` itself \u2014 no `.clone()`, `.detach()`, or arithmetic \u2014 so it is the identical object. The same early return also fires when teacher_logprobs is None or has fewer than 2 entries.",
    "correct_fact": "",
    "cluster": "mt_opd_kernels"
  },
  {
    "claim_id": "C7",
    "claim": "compute_teacher_conflict_metrics returns an empty dict when fewer than 2 teachers are passed; its conflict_nats parameter defaults to 1.0 (:498).",
    "verdict": "CONFIRMED",
    "evidence": "Signature default, mt_opd.py:494-499: `def compute_teacher_conflict_metrics(` / `    teacher_logprobs: list[torch.Tensor],` / `    response_mask: torch.Tensor,` / `    domains: Sequence[str] | np.ndarray | None = None,` / `    conflict_nats: float = 1.0,` / `) -> dict[str, float]:` \u2014 line 498 is exactly `    conflict_nats: float = 1.0,`. Early return, mt_opd.py:525-526: `    if teacher_logprobs is None or len(teacher_logprobs) < 2:` / `        return {}` (docstring line 518-519 matches: \"Empty if fewer than two teachers, since disagreement is undefined for one.\").",
    "correct_fact": "",
    "cluster": "mt_opd_kernels"
  },
  {
    "claim_id": "C8",
    "claim": "ray_trainer.py MT-OPD reward block at :2100-2230 with sub-steps (a)-(h) in the stated order and line ranges.",
    "verdict": "CONFIRMED",
    "evidence": "/data/ycfeng/Open-MOPD/training/verl/verl/trainer/ppo/ray_trainer.py \u2014 block opens at 2100 `elif reward_mode == \"mt_opd\":` and its last statement is 2230, with 2232 `elif top_k > 0:` next, so 2100-2230 is exact.\n(a) 2102-2107: `for i, extra_wg in enumerate(self.mt_rm_wgs, start=1):` / 2104 `extra_raw = extra_wg.compute_rm_score(batch)` / 2105-2107 `batch = batch.union(DataProto.from_dict(tensors={ f\"mt_teacher_{i}_on_student_log_probs\": extra_raw.batch[\"teacher_on_student_log_probs\"], }))` \u2014 rename confirmed. mt_rm_wgs is built at 1493-1496 `for i in range(1, self.mt_n_additional + 1): wg = all_wg[f\"mt_rm_{i}\"] ... self.mt_rm_wgs.append(wg)`, comment at 1484 `# additional RL teacher worker groups (index 1..N-1)`, so \"teachers 1..N-1\" matches.\n(b) 2109-2126: 2109 `domains = batch.non_tensor_batch.get(\"domain\", None)`; 2110-2114 `if domains is None: raise ValueError(\"reward_mode=mt_opd requires non_tensor_batch['domain'] on the training batch\")`; 2121-2126 `if len(teacher_logps) != len(self.mt_teacher_domains): raise ValueError(f\"MT-OPD teacher count mismatch: ...\")` where self.mt_teacher_domains comes from mt_opd.teacher_domains (line 662).\n(c) 2128-2136: 2128 `domain_weights = build_domain_weights(` then 2134 `routed_lp = select_routed_teacher_logprobs(` \u2014 order and range exact.\n(d) 2140-2147: `routed_lp, conflict_token_weight, _cm = apply_teacher_conflict_policy(...)`; 2148 is `metrics.update(_cm)` (the call itself ends at 2147, not 2148).\n(e) 2149: `batch.batch[\"teacher_on_student_log_probs\"] = routed_lp` \u2014 direct assignment, replacing the key teacher 0 was read from at 2116. (Note: the list element teacher_logps[0] still references the pre-overwrite tensor, so later metrics at 2177 still see original teacher 0.)\n(f) 2152: `distillation_output = self.actor_rollout_wg.compute_distillation_reward(batch)`.\n(g) 2202-2215: `dom_w = compute_domain_loss_weights(` ... closing `)` at 2215.\n(h) 2223 `if dom_w is not None:` / 2224 `batch.batch[\"domain_loss_weight\"] = dom_w`; the assignment is line 2224 alone, 2225-2230 is the per-domain `mt_opd/domain/{dom}/loss_weight` logging loop. Ranges and ordering as claimed.",
    "correct_fact": "",
    "cluster": "ray_trainer_mtopd_block"
  },
  {
    "claim_id": "C9",
    "claim": "ray_trainer.py:2177-2183 calls compute_teacher_conflict_metrics WITHOUT conflict_nats, so the metric threshold is pinned at the kernel default 1.0 regardless of mt_opd.conflict_nats.",
    "verdict": "CONFIRMED",
    "evidence": "ray_trainer.py:2177-2183:\n`metrics.update(`\n`    compute_teacher_conflict_metrics(`\n`        teacher_logprobs=teacher_logps,`\n`        response_mask=response_mask,`\n`        domains=domains,`\n`    )`\n`)`\n\u2014 only three kwargs, no conflict_nats. Kernel /data/ycfeng/Open-MOPD/training/verl/verl/workers/actor/mt_opd.py:494-499 `def compute_teacher_conflict_metrics(teacher_logprobs..., response_mask..., domains...=None, conflict_nats: float = 1.0)` and mt_opd.py:538 `contested = (spread > conflict_nats).float()`. self.mt_conflict_nats (set at ray_trainer.py:754-756) is passed only to apply_teacher_conflict_policy at 2145 `conflict_nats=self.mt_conflict_nats,`. So mt_opd/conflict/contested_frac always uses 1.0 nats. (The policy's own effective threshold is reported separately as mt_opd/conflict/policy_threshold_nats, mt_opd.py:473.)",
    "correct_fact": "",
    "cluster": "ray_trainer_mtopd_block"
  },
  {
    "claim_id": "C10",
    "claim": "ray_trainer.py ~:2218-2222 collapses the M3 [B,T] token weight to a per-sequence mean, multiplies it into the domain weight, and does NOT re-normalize the product.",
    "verdict": "CONFIRMED",
    "evidence": "ray_trainer.py:2218-2222:\n`if conflict_token_weight is not None:`\n`    per_seq = (`\n`        conflict_token_weight * response_mask`\n`    ).sum(dim=-1) / response_mask.sum(dim=-1).clamp(min=1.0)`\n`    dom_w = per_seq if dom_w is None else dom_w * per_seq`\nNext statements are 2223 `if dom_w is not None:` and 2224 `batch.batch[\"domain_loss_weight\"] = dom_w` \u2014 no renormalization between the multiply and the assignment. Token weight is [B,T] per mt_opd.py:435-436 (\"``token_weight`` is ``[B, T]``\") and mt_opd.py:477 `weight = agree.to(routed_logprobs.dtype)` on a [B,T] `agree`. This matters because compute_domain_loss_weights returns a mean-1 tensor by construction \u2014 mt_opd.py:332-335 `# Token-weighted mean of 1: preserves total loss magnitude ...` / `denom = (weights * per_seq).sum().clamp(min=1e-6)` / `weights = weights * (total / denom)` \u2014 and multiplying by per_seq at the call site breaks that property without restoring it.",
    "correct_fact": "",
    "cluster": "ray_trainer_mtopd_block"
  },
  {
    "claim_id": "C11",
    "claim": "_get_gen_batch (~:1145-1165) has a keep-set including \"domain\", so \"domain\" stays on batch and is NOT in gen_batch in sync mode.",
    "verdict": "CONFIRMED",
    "evidence": "ray_trainer.py:1145 `def _get_gen_batch(self, batch: DataProto) -> DataProto:` through 1165 `return gen_batch`. Keep-set at 1149-1151: `reward_model_keys = ( {\"data_source\", \"reward_model\", \"extra_info\", \"uid\", \"domain\"} & batch.non_tensor_batch.keys() )`. Pop-set at 1155 `non_tensor_batch_keys_to_pop = set(batch.non_tensor_batch.keys()) - reward_model_keys`, popped at 1156-1159, so \"domain\" is excluded from the pop and remains on `batch`. It reaches gen_batch only in async mode, 1162-1163 `if self.async_rollout_mode: gen_batch.non_tensor_batch.update(batch.non_tensor_batch)`. Comment at 1146-1148 states the intent explicitly (\"Keep routing metadata on the original training batch... MT-OPD still needs the per-row domain after rollout\").",
    "correct_fact": "",
    "cluster": "ray_trainer_mtopd_block"
  },
  {
    "claim_id": "C12",
    "claim": "__init__ mt_opd validation at :660-777 raises ValueError at 666, 681, 699, 714, 723, 731, 735, 741, 750, 762 \u2014 how many are really raises?",
    "verdict": "CONFIRMED",
    "evidence": "All 10 cited lines are real `raise ValueError` statements. `grep -n \"raise ValueError\" ray_trainer.py` returns, in the 660-777 window, exactly: 666, 681, 699, 714, 723, 731, 735, 741, 750, 762 \u2014 no more, no fewer. Contents: 666 teacher_domains unset; 681 target_share_domains/values length mismatch; 699 target_gradient_shares names unknown domains; 714 normalize_reward_scale alpha outside [0,2]; 723 reward_scale_direction not divide|multiply; 731 reward_scale_anchored requires multiply; 735 reward_scale_anchored requires reward_scale_stat=mean; 741 normalize_reward_scale needs target_gradient_shares; 750 conflict_policy not none|mask|consensus; 762 conflict_quantile outside (0,1). Two nuances on the cited *range*: the mt_opd if-block is 660-765 and 766-777 is the else-branch of plain defaults (not validation), and one further mt_opd validation sits just above the range at 657-658 `if self.use_mt_opd and not self.use_rm: raise ValueError(\"reward_mode=mt_opd requires the reward model role for teacher scoring\")`.",
    "correct_fact": "",
    "cluster": "ray_trainer_mtopd_block"
  },
  {
    "claim_id": "C13",
    "claim": "The else-branch at :766-777 (non-mt_opd) fails to set self.mt_reward_scale_stat while setting its 11 siblings.",
    "verdict": "CONFIRMED",
    "evidence": "ray_trainer.py:766 `else:` with exactly 11 assignments at 767-777: mt_teacher_domains, mt_n_additional, mt_domain_weighting, mt_target_gradient_shares, mt_normalize_reward_scale, mt_reward_scale_direction, mt_reward_scale_anchored, _mt_reward_anchor, mt_conflict_policy, mt_conflict_nats, mt_conflict_quantile. The if-branch sets 12 attributes; the one absent from the else is line 718 `self.mt_reward_scale_stat = OmegaConf.select(mt_opd_cfg, \"reward_scale_stat\") or \"mean\"`. Full-repo grep for `mt_reward_scale_stat` yields only 718 (set), 734 (read, inside the if-branch), and 2208 `reward_scale_stat=self.mt_reward_scale_stat,` (read, inside the reward_mode==\"mt_opd\" branch) \u2014 so the omission is latent rather than a live AttributeError on non-mt_opd runs, but the asymmetry claimed is exactly as stated.",
    "correct_fact": "",
    "cluster": "ray_trainer_mtopd_block"
  },
  {
    "claim_id": "C14",
    "claim": "dp_actor.py applies domain_loss_weight by multiplying into advantages (~:1128-1132), not response_mask, rank-generic via dw.view(-1, *([1]*(dim-1)))",
    "verdict": "CONFIRMED",
    "evidence": "/data/ycfeng/Open-MOPD/training/verl/verl/workers/actor/dp_actor.py:1128-1132: `if \"domain_loss_weight\" in model_inputs:` / `    _dw = model_inputs[\"domain_loss_weight\"].to(advantages.dtype)` / `    advantages = advantages * _dw.view(` / `        -1, *([1] * (advantages.dim() - 1))` / `    )`. Exact lines as cited. The comment at :1120-1127 states the rationale (\"Applied to the advantage rather than to response_mask ... response_mask is also the averaging mask for ppo_kl and pg_clipfrac\"). Repo-wide grep for `domain_loss_weight` in training/verl/verl/ returns only mt_opd.py:19/159 (compute_domain_loss_weights), dp_actor.py:975-976 (select_keys), dp_actor.py:1128-1129 (this site), ray_trainer.py:79/2202/2224 (production of the tensor) \u2014 no site multiplies it into response_mask. The `.view(-1, *([1]*(advantages.dim()-1)))` form is rank-generic (dim 3 -> view(-1,1,1); dim 2 -> view(-1,1)).",
    "correct_fact": "",
    "cluster": "actor_and_m4"
  },
  {
    "claim_id": "C15",
    "claim": "M4 (refresh_opd_advantage) is unreachable through RayPPOTrainer.fit because ray_trainer pops teacher_on_student_log_probs (~:3478-3489) before update_actor (~:3527), while dp_actor requires that key at :985 and :1095-1099",
    "verdict": "CONFIRMED",
    "evidence": "ray_trainer.py:3478-3490: `# Pop unused keys to save memory before PPO update` / `keys_to_pop = [` / `    \"teacher_on_student_log_probs\",` / `    \"teacher_top_k_ids\", ... ]` / `for key in keys_to_pop:` / `    if key in batch.batch.keys():` / `        batch.batch.pop(key)` \u2014 the exact key is the first entry (:3480), and the pop is real (`batch.batch.pop(key)` on the TensorDict). ray_trainer.py:3527: `actor_output = self.actor_rollout_wg.update_actor(batch)` \u2014 the ONLY update_actor call site in the whole repo trainer layer (grep `update_actor(` over training/verl/verl/ gives just this call plus the worker-side definitions at fsdp_workers.py:910, megatron_workers.py:619, roles/actor.py:141). Indentation scan shows the pop block sits unconditionally inside `class RayPPOTrainer:` (578) -> `def fit(self)` (1691) -> `for epoch ...` (1870) -> `for batch_dict ...` (1871) -> `with marked_timer(\"step\", timing_raw)` (1897) \u2014 no enclosing `if`. Lines 3491-3526 (critic update, adaptive KL coef bookkeeping) never re-add the key. Gate sites confirmed: dp_actor.py:985 `if self._opd_refresh_advantage and \"teacher_on_student_log_probs\" in data.batch.keys():` and dp_actor.py:1095-1099 `if (` / `self._opd_refresh_advantage` / `and not on_policy` / `and \"teacher_on_student_log_probs\" in model_inputs` / `):` guarding `refresh_opd_advantage(...)` at :1100. fsdp_workers.py:923 confirms update_actor -> `self.actor.update_policy(data=data)`. So the M4 branch and its metric `mt_opd/m4_advantage_refreshed` (:1108) can never fire on this path.",
    "correct_fact": "",
    "cluster": "actor_and_m4"
  },
  {
    "claim_id": "C16",
    "claim": "compute_distillation_reward spans :601-774; only_stu branch (~:710-714) computes rm_scores = -(student_logp - teacher_on_student) * softmax_K(weights)",
    "verdict": "CONFIRMED",
    "evidence": "dp_actor.py:601 `def compute_distillation_reward(self, data: DataProto) -> DataProto:`; body ends at :773-774 `res_tensors[\"rm_scores\"] = rm_scores` / `return DataProto.from_dict(tensors=res_tensors)` (next def at :777). only_stu at :710-714: `if strategy == \"only_stu\":` / `    kl_val = S_logp - T_on_S` / `    valid_mask = torch.ones_like(S_logp, dtype=torch.bool)` / `    norm_weights = compute_reward_weights(S_logp, T_on_S, valid_mask, reward_weight_mode)` / `    rm_scores = -kl_val * norm_weights`. The softmax-over-K claim holds: compute_reward_weights defaults `normalize=True` (:672) and does `norm_log_weights = log_probs - torch.logsumexp(log_probs, dim=-1, keepdim=True)` / `weights = torch.exp(norm_log_weights)` (:699-700), with `log_probs` selected by reward_weight_mode (S_logp for student_p, T_on_S for teacher_p, zeros for none, :686-694). T_on_S comes from `data.batch[\"teacher_on_student_log_probs\"]` (:663), S_logp from `data.batch[\"student_top_k_log_probs\"]` (:662).",
    "correct_fact": "",
    "cluster": "actor_and_m4"
  },
  {
    "claim_id": "C17",
    "claim": "top_k_strategy=\"top_p_intersec\" has no branch in compute_distillation_reward, so it hits UnboundLocalError ~:772, though config/rollout.py:155 declares it",
    "verdict": "CONFIRMED",
    "evidence": "The branch chain in compute_distillation_reward covers only: :710 `if strategy == \"only_stu\":`, :716 `elif strategy == \"only_tch\":`, :723 `elif strategy == \"intersection\":`, :730 `elif strategy == \"union\":`, :751 `elif strategy == \"union-intersection\":` \u2014 no top_p_intersec case and no else/raise. `rm_scores` is then read unconditionally; the unbound read is at :773 `res_tensors[\"rm_scores\"] = rm_scores` (:772 is a whitespace-only line), i.e. one line below the cited spot. Declaration confirmed at /data/ycfeng/Open-MOPD/training/verl/verl/workers/config/rollout.py:155 `top_k_strategy: str = \"only_stu\"  # \"only_stu\", \"only_tch\", \"intersection\", \"union\", or \"top_p_intersec\"` (plus :156 `top_p_intersec_p: float = 0.99`). Reachability: strategy flows unvalidated from config to meta_info (ray_trainer.py:1969 `strategy = self.config.actor_rollout_ref.rollout.get(\"top_k_strategy\", \"only_stu\")`, :1982 `batch.meta_info[\"top_k_strategy\"] = strategy`) and is read back at dp_actor.py:613 `strategy = data.meta_info.get(\"top_k_strategy\", \"only_stu\")`; compute_distillation_reward is invoked at ray_trainer.py:2007 (reward_mode=exopd) and :2237 (`elif top_k > 0` plain OPD path), with no guard anywhere rejecting top_p_intersec for those paths (grep `top_k_strategy` shows no validation site). Note: the sibling compute_delta_opd_reward DOES handle it (dp_actor.py:785 and :818 `if strategy in {\"intersection\", \"top_p_intersec\", \"top-p-intersec\"} ...`), so the gap is specific to compute_distillation_reward.",
    "correct_fact": "",
    "cluster": "actor_and_m4"
  },
  {
    "claim_id": "C18",
    "claim": "_optimizer_step (~:839-858) skips optimizer.step() when grad norm is non-finite",
    "verdict": "CONFIRMED",
    "evidence": "dp_actor.py:839-858: `def _optimizer_step(self):` ... `# if grad_norm is not finite, skip the update` (:852) / `if not torch.isfinite(grad_norm):` (:853) / `    print(f\"WARN: rank {torch.distributed.get_rank()} grad_norm is not finite: {grad_norm}\")` / `    self.actor_optimizer.zero_grad()` / `else:` / `    self.actor_optimizer.step()` / `return grad_norm` (:858). Exact line range as cited; grad_norm is produced by the FSDP/FSDP2/plain clip_grad_norm_ dispatch at :842-847 and DTensor-materialized at :849-850.",
    "correct_fact": "",
    "cluster": "actor_and_m4"
  },
  {
    "claim_id": "C19",
    "claim": "fsdp_workers.py RewardModelWorker starts around :1735 and its _build_model (around :1801-1885) loads AutoModelForCausalLM (not AutoModelForTokenClassification), while the class docstring still says TokenClassification.",
    "verdict": "CONFIRMED",
    "evidence": "/data/ycfeng/Open-MOPD/training/verl/verl/workers/fsdp_workers.py:1735 `class RewardModelWorker(Worker, DistProfilerExtension):`; :1736-1738 docstring `\"\"\"` / `    Note that we only implement the reward model that is subclass of AutoModelForTokenClassification.` / `\"\"\"`; :1801 `    def _build_model(self, config):`; :1804 `        from transformers import AutoConfig, AutoModelForCausalLM`; :1837 `            reward_module = AutoModelForCausalLM.from_pretrained(`; :1885 `        return reward_module`. Repo-wide grep for AutoModelForTokenClassification in this file returns only the docstring at :1737 \u2014 no import, no instantiation.",
    "correct_fact": "",
    "cluster": "teacher_worker"
  },
  {
    "claim_id": "C20",
    "claim": "compute_rm_score (around :2662-2871) does NOT put \"rm_scores\" in its output when top_k > 0 (around :2825-2828).",
    "verdict": "CONFIRMED",
    "evidence": "/data/ycfeng/Open-MOPD/training/verl/verl/workers/fsdp_workers.py:2825-2829 branch verbatim:\n2825\t            if top_k > 0:\n2826\t                # Reward calculation is moved to ray_trainer for top_k > 0\n2827\t                # because it needs student_on_teacher_log_probs which requires another actor forward\n2828\t                rm_scores = None \n2829\t                overlap_mask = teacher_overlap_mask\nand the gate that drops the key:\n2839\t            tensors = {}\n2840\t            if rm_scores is not None:\n2841\t                tensors[\"rm_scores\"] = rm_scores\nOutput is built solely from `tensors` (:2862 `output = DataProto.from_dict(tensors=tensors)`), so \"rm_scores\" is absent whenever top_k > 0. Method span is :2664 (`def compute_rm_score(self, data: DataProto, kl_estimator=\"k1\"):`) through :2871 (`return output`) \u2014 cited start 2662 is off by 2.",
    "correct_fact": "",
    "cluster": "teacher_worker"
  },
  {
    "claim_id": "C21",
    "claim": "_compute_teacher_top_k_log_probs (around :1911-2015) chunks at 1024 rows and returns teacher log-probs gathered at the STUDENT's token ids plus overlap masks.",
    "verdict": "CONFIRMED",
    "evidence": "/data/ycfeng/Open-MOPD/training/verl/verl/workers/fsdp_workers.py:1911 `def _compute_teacher_top_k_log_probs(`; :1917 `        chunk_size=1024,`; :1934 `        for start in range(0, n_samples, chunk_size):`. Both call sites omit chunk_size (:2169-2175 and :2378-2384 pass only logits/student_ids/top_k/strategy/top_p_intersec_p), so 1024 is the effective chunk. Gather at student ids at :1980 `chunk_log_probs = torch.gather(logits_chunk, dim=-1, index=student_ids_chunk) - t_logsumexp`. Overlap masks: :1963-1964 `overlap_mask_chunk = is_in_teacher.float()` / `overlap_counts_list.append(overlap_mask_chunk)` and :1968-1969 `is_in_student = matches.any(dim=-2)` / `teacher_in_student_list.append(is_in_student.float())`. Return tuple :2013-2015 emits results, valid_counts, overlap_counts, teacher_top_k_ids, teacher_top_k_log_probs, teacher_in_student. Nuance: the literal torch.gather only runs for strategies in {only_stu, union, union-intersection, top_p_intersec, top-p-intersec} (:1971); the \"intersection\" else-branch (:1997-2009) instead does a masked max over teacher top-k values (`vals_masked = t_vals_exp.masked_fill(~matches, float('-inf'))`, :2003), which still yields teacher log-probs aligned to student token slots but -inf where the student token is outside teacher top-k.",
    "correct_fact": "",
    "cluster": "teacher_worker"
  },
  {
    "claim_id": "C22",
    "claim": "reward_model.model.input_tokenizer defaults to the interpolation of actor_rollout_ref.model.path in reward_model.yaml (around line 33), i.e. NON-null, and fsdp_workers.py:1810-1813 sets _do_switch_chat_template=True whenever it is non-null, which leads to indexing non_tensor_batch[\"raw_prompt\"] (around :2513-2519) \u2014 a key absent unless data.return_raw_chat=True (legacy_data.yaml default False).",
    "verdict": "CONFIRMED",
    "evidence": "/data/ycfeng/Open-MOPD/training/verl/verl/trainer/config/reward_model/reward_model.yaml:33 `  input_tokenizer: ${actor_rollout_ref.model.path}` (exact line as cited; non-null by default). /data/ycfeng/Open-MOPD/training/verl/verl/workers/fsdp_workers.py:1810-1814:\n1810\t        if self.config.model.input_tokenizer is None:\n1811\t            self._do_switch_chat_template = False\n1812\t        else:\n1813\t            self._do_switch_chat_template = True\n1814\t            input_tokenizer_local_path = copy_to_local(config.model.input_tokenizer, use_shm=use_shm)\nDispatch at :2691 `        if self._do_switch_chat_template:` -> :2694 `rm_data = self._switch_chat_template_token_level(data)` (def at :2479), which indexes raw_prompt at :2513 `if not isinstance(data.non_tensor_batch[\"raw_prompt\"][i], list | np.ndarray):` and :2519 `chat: list = list(data.non_tensor_batch[\"raw_prompt\"][i])`. Key is gated in /data/ycfeng/Open-MOPD/training/verl/verl/utils/dataset/rl_dataset.py:478-479 `if self.return_raw_chat:` / `row_dict[\"raw_prompt\"] = messages`, and /data/ycfeng/Open-MOPD/training/verl/verl/trainer/config/data/legacy_data.yaml:59 `return_raw_chat: False`. Corroborating: the OPD launch scripts explicitly override it (/data/ycfeng/Open-MOPD/scripts/local/mt_opd.sh:109 `\"+mt_reward_model_${teacher_index}.model.input_tokenizer=null\"`).",
    "correct_fact": "",
    "cluster": "teacher_worker"
  },
  {
    "claim_id": "C23",
    "claim": "compute_distillation_reward is registered ONLY on the FSDP worker (fsdp_workers.py around :1093) and is absent from megatron_workers.py, so Megatron cannot run any OPD mode and fails at ray_trainer.py:2152.",
    "verdict": "IMPRECISE",
    "evidence": "First two parts CONFIRMED: /data/ycfeng/Open-MOPD/training/verl/verl/workers/fsdp_workers.py:1091-1093:\n1091\t    @register(dispatch_mode=make_nd_compute_dataproto_dispatch_fn(mesh_name=\"actor\"))\n1092\t    @DistProfiler.annotate(color=\"blue\", role=\"actor_compute_distillation_reward\")\n1093\t    def compute_distillation_reward(self, data: DataProto):\nand `grep -n distillation /data/ycfeng/Open-MOPD/training/verl/verl/workers/megatron_workers.py` returns zero hits (megatron ActorRolloutRefWorker at :175 exposes only `def compute_log_prob` at :734); megatron_actor.py likewise has no compute_distillation_reward. Megatron is reachable (/data/ycfeng/Open-MOPD/training/verl/verl/trainer/main_ppo.py:135-136, :211-212). The failure-site detail is wrong: :2152 is only the reward_mode==\"mt_opd\" branch. /data/ycfeng/Open-MOPD/training/verl/verl/trainer/ppo/ray_trainer.py has three call sites \u2014 :2007 (exopd), :2152 (mt_opd, gated by `elif reward_mode == \"mt_opd\":` at :2100), :2237 (standard OPD, gated by `elif top_k > 0:` at :2232). Moreover, under mt_opd a Megatron run breaks before :2152, at :2106 `f\"mt_teacher_{i}_on_student_log_probs\": extra_raw.batch[\"teacher_on_student_log_probs\"],` because megatron RewardModelWorker.compute_rm_score (/data/ycfeng/Open-MOPD/training/verl/verl/workers/megatron_workers.py:1268-1275) just calls `self.rm.compute_reward(data)` with no top-k / teacher-logprob support.",
    "correct_fact": "compute_distillation_reward exists only on the FSDP ActorRolloutRefWorker (fsdp_workers.py:1091-1093) and on dp_actor.py:601; it is absent from megatron_workers.py and megatron_actor.py, so no OPD mode works under strategy=megatron. The failing line depends on reward_mode: ray_trainer.py:2007 for exopd, :2152 for mt_opd, :2237 for standard OPD (top_k>0). For mt_opd the Megatron run actually dies earlier at ray_trainer.py:2106 (KeyError on \"teacher_on_student_log_probs\"), since megatron_workers.py:1268 compute_rm_score returns only the vanilla reward output.",
    "cluster": "teacher_worker"
  },
  {
    "claim_id": "C24",
    "claim": "core_algos.py registers \"token_reward_direct\" at :876 and its body is advantages = token_level_rewards * response_mask.unsqueeze(-1); returns = advantages.clone().",
    "verdict": "IMPRECISE",
    "evidence": "/data/ycfeng/Open-MOPD/training/verl/verl/trainer/ppo/core_algos.py:876 `@register_adv_est(\"token_reward_direct\")`, :877 `def compute_token_reward_direct_advantage(`. Body :895-902:\n```\n    with torch.no_grad():\n        # If rewards are 3D (batch, seq_len, k), broadcast mask to (batch, seq_len, 1)\n        if token_level_rewards.dim() == 3:\n            response_mask = response_mask.unsqueeze(-1)\n        advantages = token_level_rewards * response_mask\n        returns = advantages.clone()\n\n    return advantages, returns\n```\nSignature (:878-882) includes `**kwargs` and `config: Optional[AlgoConfig] = None`.",
    "correct_fact": "Registration line :876 and `returns = advantages.clone()` are exact. The multiplication is `advantages = token_level_rewards * response_mask` (:899); the `.unsqueeze(-1)` is applied to response_mask only inside a conditional `if token_level_rewards.dim() == 3:` (:897-898), i.e. only for 3-D (dense top-k) rewards, not unconditionally. The whole computation is wrapped in `with torch.no_grad()` (:895).",
    "cluster": "core_algos"
  },
  {
    "claim_id": "C25",
    "claim": "\"token_reward_direct_plus_grpo\" is registered at :905 and returns a 3-tuple, using _scatter_outcome_adv_onto_sampled_topk (around :970).",
    "verdict": "CONFIRMED",
    "evidence": "/data/ycfeng/Open-MOPD/training/verl/verl/trainer/ppo/core_algos.py:905 `@register_adv_est(\"token_reward_direct_plus_grpo\")`, :906 `def compute_token_reward_direct_plus_grpo_advantage(`. 3-tuple return at :967 `    return combined_adv, combined_returns, extra` (preceded by :965 `combined_returns = combined_adv.clone()`). Helper invoked at :952-958 `grpo_adv_3d, sampled_frac = _scatter_outcome_adv_onto_sampled_topk(` inside the `if direct_adv.dim() == 3 and grpo_adv.dim() == 2:` branch (:942), and defined at :970 `def _scatter_outcome_adv_onto_sampled_topk(`. The declared return annotation is still `-> tuple[torch.Tensor, torch.Tensor]` (:912) while the function actually returns 3 values; the caller handles this via `len(res) == 3` in ray_trainer.py:562-566.",
    "correct_fact": "",
    "cluster": "core_algos"
  },
  {
    "claim_id": "C26",
    "claim": "\"token_grpo\" is registered around :352 but is unreachable because its signature lacks **kwargs while the caller always forwards true_reward_score. Verify the signature and the caller.",
    "verdict": "CONFIRMED",
    "evidence": "Registration + signature, /data/ycfeng/Open-MOPD/training/verl/verl/trainer/ppo/core_algos.py:352-360:\n```\n@register_adv_est(\"token_grpo\")\ndef compute_grpo_process_advantage(\n    token_level_rewards: torch.Tensor,\n    response_mask: torch.Tensor,\n    index: np.ndarray,\n    epsilon: float = 1e-6,\n    norm_adv_by_std_in grpo: bool = True,   # actual: norm_adv_by_std_in_grpo\n    config: Optional[AlgoConfig] = None,\n) -> tuple[torch.Tensor, torch.Tensor]:\n```\nNo `**kwargs`. Caller /data/ycfeng/Open-MOPD/training/verl/verl/trainer/ppo/ray_trainer.py:548-558:\n```\n        if \"true_reward_score\" in data.batch: # optional\n            adv_kwargs[\"true_reward_score\"] = data.batch[\"true_reward_score\"]\n...\n        if \"student_top_k_ids\" in data.batch:  # optional\n            adv_kwargs[\"student_top_k_ids\"] = data.batch[\"student_top_k_ids\"]\n        if \"responses\" in data.batch:  # optional\n            adv_kwargs[\"responses\"] = data.batch[\"responses\"]\n```\nthen :561 `res = adv_estimator_fn(**adv_kwargs)`. The key is guaranteed present in the fit loop: ray_trainer.py:2475-2487 sets it on every step, with the unconditional fallback at :2486-2487 `else:` / `batch.batch[\"true_reward_score\"] = reward_tensor`. `responses` is likewise always present (used unconditionally at ray_trainer.py:463 `responses = data.batch[\"responses\"]` in compute_response_mask). So `**adv_kwargs` carries at least two keys the signature cannot accept -> TypeError. Additionally `token_grpo` appears nowhere else in the repo (only the :352 registration; no enum member in AdvantageEstimator at core_algos.py:97-107 and no config/script selects it).",
    "correct_fact": "",
    "cluster": "core_algos"
  },
  {
    "claim_id": "C27",
    "claim": "The 3-D policy-loss branch is around :1214-1251 and sums over the K axis (pg_losses = ...sum(dim=-1)) around :1240; pg_clipfrac_lower is hard-coded 0.0 around :1251.",
    "verdict": "CONFIRMED",
    "evidence": "/data/ycfeng/Open-MOPD/training/verl/verl/trainer/ppo/core_algos.py, inside compute_policy_loss_vanilla (registered `@register_policy_loss(\"vanilla\")` at :1152): :1214 `    if log_prob.dim() == 3 and old_log_prob.dim() == 3:`; :1240 `        pg_losses = torch.sum(pg_losses, dim=-1) # [MOD] for wings, sum across wings-tokens`; :1251 `            pg_clipfrac_lower = torch.tensor(0.0, device=log_prob.device) # Placeholder`; branch ends at :1252 `    else:` / :1253 `        # Standard 2D case`. Also in-branch: :1215 `        print(f\"Advantages shape: {advantages.shape}\")` (unguarded print per micro-batch) and :1246 `ppo_kl = verl_F.masked_mean(-negative_approx_kl.sum(dim=-1), response_mask)`.",
    "correct_fact": "",
    "cluster": "core_algos"
  },
  {
    "claim_id": "C28",
    "claim": "actor.yaml sets kl_loss_coef: 2.5 and adaptive_kl_loss_max_coef: 2.5 (equal), so the adaptive controller starts at its own ceiling.",
    "verdict": "CONFIRMED",
    "evidence": "/data/ycfeng/Open-MOPD/training/verl/verl/trainer/config/actor/actor.yaml:85 `kl_loss_coef: 2.5` and :92 `adaptive_kl_loss_max_coef: 2.5` (with :88 `adaptive_kl_loss_coef: false`, :90 `adaptive_kl_loss_eps: 0.02`, :91 `adaptive_kl_loss_min_coef: 0.5`). The controller does seed from kl_loss_coef and clamp at max_coef: ray_trainer.py:3512 `old_kl_coef = float(actor_config.kl_loss_coef)`, :3513 `new_kl_coef = update_kl_loss_coef_from_reward(current_coef=old_kl_coef, ...)`, :3518 `max_coef=float(actor_config.get(\"adaptive_kl_loss_max_coef\", 2.5)),`, :3520 `actor_config.kl_loss_coef = new_kl_coef`. kl_controller.py:8-14 shows the update is one-sided-clamped: `if reward_mean < 0: new_coef = current_coef * (1 - eps) elif reward_mean > 0: new_coef = current_coef * (1 + eps)` then `return max(min_coef, min(max_coef, new_coef))` \u2014 so starting at 2.5 the positive-reward branch is a no-op and the coefficient can only ever move down. Scope note (not a contradiction): the gate at ray_trainer.py:3505-3507 requires both `use_kl_loss` (actor.yaml:78 `use_kl_loss: false`) and `adaptive_kl_loss_coef` (:88 `false`), so with stock defaults the controller never runs; the equal-value condition bites only once both are enabled. No other .sh/.yaml in the repo (outside training/verl/recipe/) overrides these values.",
    "correct_fact": "",
    "cluster": "core_algos"
  },
  {
    "claim_id": "C29",
    "claim": "sft_trainer.py uses hydra.main(config_name=\"sft_trainer_engine\") and always builds MultiTurnSFTDataset unless data.custom_cls.path is set; confirm line numbers.",
    "verdict": "CONFIRMED",
    "evidence": "/data/ycfeng/Open-MOPD/training/verl/verl/trainer/sft_trainer.py:378 `@hydra.main(config_path=\"config\", config_name=\"sft_trainer_engine\", version_base=None)`; :383 `def create_sft_dataset(data_paths, data_config, tokenizer, max_samples=-1):`; :387 `if data_config.custom_cls.get(\"path\", None):`; :390 `dataset_cls = load_extern_type(data_config.custom_cls.path, data_config.custom_cls.name)`; :391-393 `else:` / `# Default to multi-turn dataset` / `dataset_cls = MultiTurnSFTDataset`; :37 `from verl.utils.dataset.multiturn_sft_dataset import MultiTurnSFTDataset`. create_sft_dataset is the only dataset factory used, called at :144 (train) and :148 (val).",
    "correct_fact": "",
    "cluster": "sft_and_checkpoints"
  },
  {
    "claim_id": "C30",
    "claim": "MultiTurnSFTDataset reads messages column from config[\"multiturn\"][\"messages_key\"], while sft_trainer_engine.yaml declares messages_key FLAT under data:, so the flat key is dead and the effective column is the hard default \"messages\".",
    "verdict": "CONFIRMED",
    "evidence": "/data/ycfeng/Open-MOPD/training/verl/verl/utils/dataset/multiturn_sft_dataset.py:62-66 `# Get messages_key from the new multiturn config structure` / `multiturn_config = config.get(\"multiturn\", {})` / `self.messages_key = multiturn_config.get(\"messages_key\", \"messages\")` / `self.tools_key = multiturn_config.get(\"tools_key\", \"tools\")` / `self.enable_thinking_key = multiturn_config.get(\"enable_thinking_key\", \"enable_thinking\")`, used at :117 `self.messages = self.dataframe[self.messages_key].apply(series_to_item).tolist()`. /data/ycfeng/Open-MOPD/training/verl/verl/trainer/config/sft_trainer_engine.yaml:24-27 has `# Multi-turn settings` then `messages_key: messages`, `tools_key: tools`, `enable_thinking_key: enable_thinking` at the same indentation as other `data:` fields, with NO `multiturn:` block anywhere in the file (whole 70-line file read). Contrast: the legacy /data/ycfeng/Open-MOPD/training/verl/verl/trainer/config/sft_trainer.yaml:19-23 does nest them under `multiturn:`. Repo-wide grep for `messages_key` finds only those two yaml files plus multiturn_sft_dataset.py lines 62/64/117 \u2014 no code reads data.messages_key, and no script under /data/ycfeng/Open-MOPD/scripts or /data/ycfeng/Open-MOPD/training/scripts overrides data.multiturn.messages_key. So tools_key and enable_thinking_key are dead the same way.",
    "correct_fact": "",
    "cluster": "sft_and_checkpoints"
  },
  {
    "claim_id": "C31",
    "claim": "checkpoint.save_contents including 'hf_model' does NOT work for the SFT stage: transformer_impl.py:159-165 passes the config as checkpoint_contents=, which FSDPCheckpointManager.__init__ swallows into **kwargs, leaving checkpoint_config=None.",
    "verdict": "CONFIRMED",
    "evidence": "Caller /data/ycfeng/Open-MOPD/training/verl/verl/workers/engine/fsdp/transformer_impl.py:159-165 `self.checkpoint_manager = FSDPCheckpointManager(` / `model=self.module,` / `optimizer=self.optimizer,` / `lr_scheduler=self.lr_scheduler,` / `processing_class=self.model_config.get_processor(),` / `checkpoint_contents=self.checkpoint_config,` / `)` \u2014 and `self.checkpoint_config` is the real CheckpointConfig (:93 `checkpoint_config: CheckpointConfig,` :108 `self.checkpoint_config = checkpoint_config`). Callee /data/ycfeng/Open-MOPD/training/verl/verl/utils/checkpoint/fsdp_checkpoint_manager.py:75-96 signature is `def __init__(self, model, optimizer=None, lr_scheduler=None, processing_class=None, checkpoint_config: DictConfig = None, **kwargs)`; the only kwargs key handled is `tokenizer` (:84 `if processing_class is None and \"tokenizer\" in kwargs:`), then it forwards `checkpoint_config=checkpoint_config` (=None) to super. Base /data/ycfeng/Open-MOPD/training/verl/verl/utils/checkpoint/checkpoint_manager.py:52-57 then falls back: `checkpoint_save_contents = checkpoint_config.get(\"save_contents\", None) if checkpoint_config else None` ... `if checkpoint_save_contents is None: checkpoint_save_contents = [\"model\", \"optimizer\", \"extra\"]`, so :93-98 `should_save_hf_model` -> `\"hf_model\" in self.checkpoint_save_contents` is always False and the hf-weight branch at fsdp_checkpoint_manager.py:308 `if self.should_save_hf_model:` never runs. `checkpoint_contents=` appears exactly once repo-wide (grep hit only transformer_impl.py:164); the other three call sites (trainer/fsdp_sft_trainer.py:595-600, workers/fsdp_workers.py:887, :900, :1639) correctly pass `checkpoint_config=`. This is the SFT path actually used: /data/ycfeng/Open-MOPD/scripts/local/sft.sh:48 launches `-m verl.trainer.sft_trainer`, whose trainer builds the engine with `checkpoint_config=self.checkpoint_config` (sft_trainer.py:106,111,117).",
    "correct_fact": "",
    "cluster": "sft_and_checkpoints"
  },
  {
    "claim_id": "C32",
    "claim": "SFT layout under default_local_dir/global_step_N/: model|optim|extra_state_world_size_{WS}_rank_{R}.pt, fsdp_config.json, huggingface/ (config + tokenizer, no weights), data_{dp_rank}.pt; plus latest_checkpointed_iteration.txt at the parent. PPO is one level deeper under global_step_N/actor/ with a single data.pt.",
    "verdict": "CONFIRMED",
    "evidence": "fsdp_checkpoint_manager.py:225-227 `model_path = os.path.join(local_path, f\"model_world_size_{self.world_size}_rank_{self.rank}.pt\")` / `optim_path = ... f\"optim_world_size_{self.world_size}_rank_{self.rank}.pt\"` / `extra_path = ... f\"extra_state_world_size_{self.world_size}_rank_{self.rank}.pt\"`; :258 `hf_config_tokenizer_path = os.path.join(local_path, \"huggingface\")` with :250-251 comment `Save HF tokenizer/processor and model config on rank 0 to huggingface/ directory, no matter whether huggingface model is requested to be saved or not.` and only `model_config.save_pretrained` (:280) + `self.processing_class.save_pretrained` (:282) (+ generation_config at :278) \u2014 weights only inside `if self.should_save_hf_model:` (:308), which is dead per C31; :296 `fsdp_config_path = os.path.join(local_path, \"fsdp_config.json\")`. Path/step layout and dataloader: /data/ycfeng/Open-MOPD/training/verl/verl/utils/checkpoint/checkpoint_handler.py:70 `local_global_step_folder = os.path.join(self.default_local_dir, f\"global_step_{step}\")`; :86-89 `if self.engine.is_mp_src_rank_with_outputs():` ... `dataloader_local_path = os.path.join(local_global_step_folder, f\"data_{dp_rank}.pt\")`; :98 `tracker_file = get_checkpoint_tracker_filename(self.default_local_dir)` with checkpoint_manager.py:200-204 returning `os.path.join(root_path, \"latest_checkpointed_iteration.txt\")`. PPO: /data/ycfeng/Open-MOPD/training/verl/verl/trainer/ppo/ray_trainer.py:1512 `def _save_checkpoint(self):`, :1515-1517 `local_global_step_folder = os.path.join(self.config.trainer.default_local_dir, f\"global_step_{self.global_steps}\")`, :1521 `actor_local_path = os.path.join(local_global_step_folder, \"actor\")`, :1548 `dataloader_local_path = os.path.join(local_global_step_folder, \"data.pt\")`, :1554-1555 `os.path.join(self.config.trainer.default_local_dir, \"latest_checkpointed_iteration.txt\")`. Minor addition, not a contradiction: huggingface/ also gets generation_config.json when the model can generate, and data_{dp_rank}.pt is written only by ranks where is_mp_src_rank_with_outputs() is true (sp-rank-0 of each dp group).",
    "correct_fact": "",
    "cluster": "sft_and_checkpoints"
  },
  {
    "claim_id": "C33",
    "claim": "training/scripts/sft/merge_model.sh requires fsdp_config.json in the source dir and is the only in-repo documentation of the mandatory inter-stage conversion; referenced by neither README.md nor scripts/local/README.md.",
    "verdict": "CONFIRMED",
    "evidence": "/data/ycfeng/Open-MOPD/training/scripts/sft/merge_model.sh: `[[ -f \"$input_dir/fsdp_config.json\" ]] || die \"missing fsdp_config.json under $input_dir\"` (hard precondition), usage text `CHECKPOINT_DIR must contain fsdp_config.json and the checkpoint shards`, and it wraps `-m verl.model_merger merge --backend fsdp --local_dir ... --target_dir ...`; the requirement matches the consumer /data/ycfeng/Open-MOPD/training/verl/verl/model_merger/fsdp_model_merger.py:75 `config_path = Path(self.config.local_dir) / \"fsdp_config.json\"`. Reference check: grep for `merge_model|model_merger|fsdp_config` over /data/ycfeng/Open-MOPD/README.md and /data/ycfeng/Open-MOPD/scripts/local/README.md returns no hits (both files read/grepped; scripts/local/README.md is 38 lines describing only opd.sh/mt_opd.sh/eval.sh and env vars, and README.md's only huggingface mentions are hub links at :29-30, :82-90). Repo-wide `grep -rln \"merge_model.sh\"` outside task_memory hits only the script itself plus training/verl/recipe/flowrl/README.md:139 `bash command/eval/merge_model.sh`, which points at an unrelated vendored-recipe path. Nothing under /data/ycfeng/Open-MOPD/scripts, /data/ycfeng/Open-MOPD/training/scripts, /data/ycfeng/Open-MOPD/evals or /data/ycfeng/Open-MOPD/experiments references merge_model.sh or verl.model_merger (the experiments/backend/param_merge.py hits are an unrelated parameter-merge feature). Caveat on the word \"only\": vendored verl docs do mention model_merger generically (training/verl/verl/models/mcore/readme.md, training/verl/recipe/open_math_reasoning/README.md); no training/verl/docs directory exists. For the Open-MOPD SFT->OPD handoff specifically, merge_model.sh is the sole in-repo artifact describing the conversion.",
    "correct_fact": "",
    "cluster": "sft_and_checkpoints"
  },
  {
    "claim_id": "C34",
    "claim": "experiments/data/build_mix_rl_raw_union.py is the ONLY in-repo builder that emits a top-level \"domain\" column; no script under training/scripts/rl/ emits it.",
    "verdict": "WRONG",
    "evidence": "First half FALSE. At least four other in-repo builders emit a top-level `domain` column:\n(1) /data/ycfeng/Open-MOPD/experiments/data/build_mix_rl_union.py:108 `record[\"domain\"] = domain` (inside `_restore_domain`, top level of the record, sibling of `record[\"extra_info\"] = extra` at :107), and that record list becomes the written frame: :137 `output = pd.DataFrame.from_records(records)` -> :181 `output.to_parquet(args.output, index=False)`. The column is read back at :139 `output.iloc[start:start+192][\"domain\"].value_counts()`, :147 `output.loc[output[\"domain\"] == domain, \"reward_model\"]`, and :186 `\"domain_counts\": output[\"domain\"].value_counts()...` -- so it is provably a top-level parquet column, in an RL builder.\n(2) /data/ycfeng/Open-MOPD/training/scripts/sft/build_weighted_mix.py:15 `OUTPUT_FIELDS = [\"messages\", \"domain\", \"source\", \"dataset\", \"split\", \"difficulty\", \"sample_id\"]`, :104 `\"domain\": str(domain),`, :138 `(\"domain\", pa.string()),` in the Arrow schema.\n(3) /data/ycfeng/Open-MOPD/training/scripts/sft/shard_sft_parquet.py:15 same OUTPUT_FIELDS list, :68 `\"domain\": str(domain),`, :100 `(\"domain\", pa.string()),`.\n(4) /data/ycfeng/Open-MOPD/experiments/data/rft_from_rollouts.py:132 `\"domain\": domain,` at top level of `record`, written via :153 `frame = pd.DataFrame(rows)` / :156 `frame.to_parquet(...)`, and sorted on at :155 `frame.sort_values([\"domain\", \"prompt_sha256\"] ...)`.\nSecond half TRUE: `grep -c domain` over ALL six files in /data/ycfeng/Open-MOPD/training/scripts/rl/ (build_dapo_math_rl_dataset.py, build_deepcoder_rl_dataset.py, build_livecodebench_rl_val_dataset.py, build_nemotron_if_rl_dataset.py, filter_nemotron_if_rl_parquet.py, ifrl_family.py) returns 0 for every file; their schemas are exactly data_source/prompt/ability/reward_model/extra_info (e.g. build_dapo_math_rl_dataset.py:66-73, build_deepcoder_rl_dataset.py:116-124, build_livecodebench_rl_val_dataset.py:70-78, build_nemotron_if_rl_dataset.py:118-122). Reference cite for the raw_union builder itself is correct: build_mix_rl_raw_union.py:64 `pa.field(\"domain\", pa.string()),` in OUTPUT_SCHEMA and :108 `\"domain\": domain,` in `_normalize_record`.",
    "correct_fact": "build_mix_rl_raw_union.py is NOT the only builder emitting a top-level `domain` column. In-repo emitters: experiments/data/build_mix_rl_raw_union.py (:64, :108), experiments/data/build_mix_rl_union.py (:108, persisted at :181), training/scripts/sft/build_weighted_mix.py (:15, :104, :138), training/scripts/sft/shard_sft_parquet.py (:15, :68, :100), experiments/data/rft_from_rollouts.py (:132). The claim's restricted form holds: it is the only builder under training/scripts/rl/... in the sense that NO script under training/scripts/rl/ mentions `domain` at all (0 occurrences in all six files), and among the two MixRL builders it is the one with an explicit Arrow OUTPUT_SCHEMA field.",
    "cluster": "data_contracts"
  },
  {
    "claim_id": "C35",
    "claim": "rl_dataset.py __getitem__ (around :338) does row_dict = self.dataframe[item] and returns the whole row, and the collate_fn (around :96-111) funnels every non-tensor column into non_tensor_batch -- this is HOW an arbitrary \"domain\" column reaches the trainer.",
    "verdict": "CONFIRMED",
    "evidence": "/data/ycfeng/Open-MOPD/training/verl/verl/utils/dataset/rl_dataset.py:334 `def __getitem__(self, item):`; :338 `row_dict: dict = self.dataframe[item]` (exact line as cited); :497 `return row_dict` -- the same dict, augmented with input_ids/attention_mask/position_ids (:459-461), raw_prompt_ids (:476), index/tools_kwargs/interaction_kwargs (:494-496); unrecognized columns such as `domain` are never filtered, so they pass through verbatim (only image_key/video_key are popped, :351/:361).\ncollate_fn is defined at :83 (`def collate_fn(data_list: list[dict]) -> dict:`), with the funnel at :95-96 `tensors = defaultdict(list)` / `non_tensors = defaultdict(list)`, :98-103 `for data in data_list: for key, val in data.items(): if isinstance(val, torch.Tensor): tensors[key].append(val) else: non_tensors[key].append(val)`, :108-109 `non_tensors[key] = np.fromiter(val, dtype=object, count=len(val))`, :111 `return {**tensors, **non_tensors}`. Cited range :96-111 is inside the function; the def itself starts at :83.\nThe `non_tensor_batch` naming is completed downstream, confirming the mechanism: /data/ycfeng/Open-MOPD/training/verl/verl/trainer/ppo/ray_trainer.py:829-831 wires this exact collate_fn as the dataloader default (`from verl.utils.dataset.rl_dataset import collate_fn as default_collate_fn`), :841/:857 pass it to the DataLoaders, then :1881 `batch: DataProto = DataProto.from_single_dict(batch_dict)`, and /data/ycfeng/Open-MOPD/training/verl/verl/protocol.py:492-500 routes every `np.ndarray` value into `non_tensors` -> `from_dict(tensors=..., non_tensors=...)`. So an object-dtype `domain` array lands in `non_tensor_batch[\"domain\"]`.",
    "correct_fact": "",
    "cluster": "data_contracts"
  },
  {
    "claim_id": "C36",
    "claim": "domain_weighted_sampler.py hardcodes DOMAIN_ORDER = (\"math\",\"code\",\"if\") around :15, raises without a top-level domain column (around :58-59), and has ZERO production callers (ray_trainer.py imports only weighted_quotas from that module).",
    "verdict": "CONFIRMED",
    "evidence": "/data/ycfeng/Open-MOPD/training/verl/verl/utils/dataset/domain_weighted_sampler.py:15 `DOMAIN_ORDER = (\"math\", \"code\", \"if\")` (exact line). :57-59 `dataframe = getattr(data_source, \"dataframe\", None)` / `if dataframe is None or \"domain\" not in dataframe.column_names:` / `raise ValueError(\"DomainWeightedSampler requires a top-level dataset 'domain' column\")` (exact lines as cited).\nZero production callers: `grep -rn \"DomainWeightedSampler\"` over the repo returns only domain_weighted_sampler.py:36 (`class DomainWeightedSampler(AbstractSampler):`), :59 (the error string), and training/verl/tests/test_domain_weighted_sampler.py:6 (import) and :45 (`sampler = DomainWeightedSampler(dataset, config)`). The single production reference to the module is /data/ycfeng/Open-MOPD/training/verl/verl/trainer/ppo/ray_trainer.py:1797 `from verl.utils.dataset.domain_weighted_sampler import weighted_quotas` (inside `def _domain_quotas(total):` at :1796, used at :1799 `return weighted_quotas(total, _dyn_domain_weights)`) -- the helper only, not the class. The only wiring path would be `data.sampler.class_path`, and both places it is declared set it to null: training/verl/verl/trainer/config/data/legacy_data.yaml:122 `class_path: null` and training/verl/recipe/gkd/config/on_policy_distill_trainer.yaml:32 `class_path: null`; no .sh in training/ or scripts/ sets it.",
    "correct_fact": "",
    "cluster": "data_contracts"
  },
  {
    "claim_id": "C37",
    "claim": "reward_manager/naive.py passes rm_scores through verbatim as the training reward and files the rule score under reward_extra_info[\"true_reward_score\"] (around :280); and reward_extra_info is REBOUND around :279, discarding per-row rule metrics.",
    "verdict": "CONFIRMED",
    "evidence": "/data/ycfeng/Open-MOPD/training/verl/verl/workers/reward_manager/naive.py:274 comment `# Caculate the reward using reward_fn first, we want to know true reward scores, but we still use rm_scores for training`; :275 `if \"rm_scores\" in data.batch.keys():`; :279 `reward_extra_info = {key: data.non_tensor_batch[key] for key in reward_extra_keys}` (the REBIND, exact line as cited); :280 `reward_extra_info[\"true_reward_score\"] = reward_tensor` (exact line as cited); :285 `return {\"reward_tensor\": data.batch[\"rm_scores\"], \"reward_extra_info\": reward_extra_info}` and :287 `return data.batch[\"rm_scores\"]` -- rm_scores verbatim, no scaling or blending.\nThe discard is real: :144 `reward_extra_info = defaultdict(list)` creates the accumulator, which is filled per row at :248-249 (overlong_reward/overlong), :251-252 (length_cap/length_capped) and :257-258 (`for key, value in score.items(): reward_extra_info[key].append(value)`); the :279 assignment replaces that object with a fresh dict built only from `data.meta_info[\"reward_extra_keys\"]`, so every accumulated per-row rule metric is dropped -- only the aggregate `reward_tensor` survives, under `true_reward_score`. Scope note: the rebind lives inside the `if return_dict:` branch of the rm_scores path; a parallel earlier short-circuit at :126-131 (`if \"rm_scores\" in data.batch.keys() and not self.compute_true_reward:`) does the same rebind at :129 but skips rule scoring altogether and never sets `true_reward_score`.",
    "correct_fact": "",
    "cluster": "data_contracts"
  },
  {
    "claim_id": "C38",
    "claim": "opd_val_dispatch.py routes IF-like and LCB-like data_sources to their verifiable scorers and everything else to ttrl_math. Confirm the three predicates.",
    "verdict": "CONFIRMED",
    "evidence": "/data/ycfeng/Open-MOPD/training/verl/verl/utils/reward_score/opd_val_dispatch.py:27-32 `def _is_if(ds: str) -> bool: return (\"ifeval\" in ds or ds.startswith(\"ifbench\") or ds in (\"nemotron_if\", \"ifbench\", \"instruction_following\"))`; :35-36 `def _is_code(ds: str) -> bool: return ds.startswith(\"livecodebench\") or ds in (\"livecodebench\", \"codecontests_lcb\")`. Routing at :48-63: `ds = str(data_source or \"\")`; :49-54 `if _is_if(ds): from verl.utils.reward_score import instruction_following; res = instruction_following.compute_score(solution_str, ground_truth, extra_info, data_source, scoring_mode=\"official_eval\")`; :55-60 `elif _is_code(ds): from verl.utils.reward_score import rllm_code_reward; res = rllm_code_reward.compute_score(data_source, solution_str, ground_truth, extra_info, scoring_mode=\"official_lcb\")`; :61-63 `else: # math + everything else: ttrl_math` -> `res = ttrl_math.reward_func(data_source, solution_str, ground_truth, extra_info)`; :64 `return _normalize(res)` which collapses output to `{\"score\", \"acc\"}` only (:67-87). Counting note: the file defines TWO predicate helpers (_is_if, _is_code) plus the else fallback, i.e. three routing branches -- the substantive routing claim matches exactly.",
    "correct_fact": "",
    "cluster": "data_contracts"
  },
  {
    "claim_id": "C39",
    "claim": "/data/ycfeng/Open-MOPD/evals/score_rollouts.py rewrites the rollout parquets IN PLACE by adding a score column (around :224-226), and its per-row code graders swallow every exception to 0.0 (around :106, :140).",
    "verdict": "CONFIRMED",
    "evidence": "/data/ycfeng/Open-MOPD/evals/score_rollouts.py:218 `df[\"score\"] = per_row`; :223-226 `# write the per-row score BACK INTO each source parquet (in-place), then it gets uploaded` / `for f in files:` / `sub = df[df[\"_src_file\"] == f].drop(columns=[\"_src_file\"])` / `sub.to_parquet(f, index=False)` \u2014 `files` comes from :40 `files = sorted(glob.glob(os.path.join(rollout_dir, \"*.parquet\")))`, so the write target is the original input file. Graders: :106-107 (in `_grade_lcb_row`) `except Exception:` / `return 0.0`; :140-141 (in `_grade_code_testcases_row`) `except Exception:` / `return 0.0`. Module docstring :1 also states \"Score eval rollout parquets IN-PLACE\" and :6 \"written BACK INTO each rollout parquet (in-place)\". Line numbers cited are exact.",
    "correct_fact": "",
    "cluster": "evals"
  },
  {
    "claim_id": "C40",
    "claim": "/data/ycfeng/Open-MOPD/evals/verifier/score.py output JSON shape is results: list of {dataset, metric, scored_rows, correct, official_total, official, live, ...}, plus macro_official and macro_live (float or null), and unscored datasets yield a ragged row with correct/official/live null and a note, without \"metric\".",
    "verdict": "CONFIRMED",
    "evidence": "/data/ycfeng/Open-MOPD/evals/verifier/score.py:111-121 `return { \"dataset\": dataset, \"metric\": metric, \"scored_rows\": scored_rows, \"correct\": correct, \"official_total\": total, \"official\": correct / total if ..., \"live\": correct / scored_rows if ..., \"official_extra\": extra, **behavior, }`. :190 `payload = {\"results\": details, \"macro_official\": None, \"macro_live\": None}`; :193-196 `if official_scores: payload[\"macro_official\"] = sum(float(x) for x in official_scores) / len(official_scores)` (same for macro_live) \u2014 so both are float when any dataset scored, else JSON null. Unscored path :99 `return build_unscored(dataset, total, \"unknown_dataset\")` and :102 `return build_unscored(dataset, total, str(result[\"error\"]))`; build_unscored at /data/ycfeng/Open-MOPD/evals/verifier/score_functions/common/utils.py:85-94 returns `{\"dataset\", \"scored_rows\": 0, \"correct\": None, \"official_total\": total, \"official\": None, \"live\": None, \"note\": note}` \u2014 no \"metric\" key. Additional detail beyond the claim: the unscored row also omits \"official_extra\" and the six behavior keys (trunc_rows/repeat_rows/trunc_repeat_rows/trunc_rate/repeat_rate/trunc_repeat_rate) produced by compute_behavior_metrics (behavior.py:79-86), so the raggedness is wider than just \"metric\".",
    "correct_fact": "",
    "cluster": "evals"
  },
  {
    "claim_id": "C41",
    "claim": "/data/ycfeng/Open-MOPD/evals/rollout_engine/vllm_rollout.py output filename pattern is {dataset}_rollouts_base{B}[_offset{O}]_rank{R}.parquet and each output row carries completion_index, completion, completion_tokens, finish_reason, stop_reason, generated_special_token_counts, plus stamped temperature/top_p/top_k/max_tokens. Also: an empty result.outputs emits a row LACKING finish_reason/stop_reason/generated_special_token_counts (ragged), around :279-285.",
    "verdict": "CONFIRMED",
    "evidence": "Filename: /data/ycfeng/Open-MOPD/evals/rollout_engine/vllm_rollout.py:304 `file_suffix = f\"base{base}_offset{offset}_rank{global_dp_rank}\" if offset else f\"base{base}_rank{global_dp_rank}\"` and :318 `out_path = output_dir / f\"{dataset}_rollouts_{file_suffix}.parquet\"`. Row fields: :288-296 `item[\"completion_index\"] = completion_index` / `item[\"completion\"] = output.text` / `item[\"completion_tokens\"] = len(output_token_ids)` / `item[\"finish_reason\"] = getattr(output, \"finish_reason\", None)` / `item[\"stop_reason\"] = getattr(output, \"stop_reason\", None)` / `item[\"generated_special_token_counts\"] = generated_special_token_counts(output_token_ids, tokenizer)`. Stamped params: :257-260 `df_slice[\"temperature\"] = temperature` / `df_slice[\"top_p\"] = top_p` / `df_slice[\"top_k\"] = top_k` / `df_slice[\"max_tokens\"] = max_tokens`. Empty-outputs row: :279-285 `if not result.outputs:` / `item = row.to_dict()` / `item[\"completion_index\"] = 0` / `item[\"completion\"] = \"\"` / `item[\"completion_tokens\"] = 0` / `outputs.append(item)` / `continue` \u2014 no finish_reason/stop_reason/generated_special_token_counts keys. Line cite :279-285 is exact. Two details the claim does not mention (neither contradicts it): (a) :262 also conditionally stamps `df_slice[\"stop_token_ids\"]`; (b) :321-324 a fallback filename `{path.stem}_rollouts_{file_suffix}.parquet` is used when the frame has no usable `dataset` column; (c) the ragged dict is later fed to :299 `pd.DataFrame.from_records(outputs)`, which back-fills the missing keys as NaN columns if at least one sibling row supplied them, so the parquet is ragged only in the all-empty case.",
    "correct_fact": "",
    "cluster": "evals"
  },
  {
    "claim_id": "C42",
    "claim": "These paths documented in /data/ycfeng/Open-MOPD/evals/README.md do NOT exist in the release: evals/rollout_engine/build_data.py, evals/misc/, evals/rollout_engine/scripts/run_all.sh, evals/verifier/third_party/, evals/data/. And evals/verifier/scripts/manage_third_party.py therefore raises FileNotFoundError even for --help (around :103).",
    "verdict": "CONFIRMED",
    "evidence": "README references: /data/ycfeng/Open-MOPD/evals/README.md:32-33 `python -m evals.rollout_engine.build_data --dataset aime24 ...`; :76 `python -m evals.misc.fetch_livecodebench_assets`; :142 `bash evals/rollout_engine/scripts/run_all.sh`; :84-85 `evals/verifier/third_party/repos.lock.json` / `evals/verifier/third_party/patches/`; :64 `evals/data/benchmark_assets/livecodebench/code_generation_lite/`, :107 `evals/data/parquet/aime24.parquet`, :147-148 defaults `evals/data/source` / `evals/data/parquet`. Existence check from repo root: MISSING evals/rollout_engine/build_data.py, evals/misc, evals/rollout_engine/scripts, evals/rollout_engine/scripts/run_all.sh, evals/verifier/third_party, evals/data. `git ls-files evals` lists only README.md, __init__.py, aggregate_lcb_scorecard.py, rollout_engine/{vllm_rollout.py,tests/test_vllm_rollout.py}, score_rollouts.py, verifier/** \u2014 no build_data.py, no misc/, no scripts/run_all.sh, no third_party/, no data/. FileNotFoundError verified by execution: `python -m evals.verifier.scripts.manage_third_party --help` -> `File \".../manage_third_party.py\", line 103, in main / choices = sorted(load_repo_lock()[\"repos\"].keys())` ... `line 34, in repo_lock_path / raise FileNotFoundError(f\"repos.lock.json not found in: {candidates}\")` -> `FileNotFoundError: repos.lock.json not found in: [PosixPath('/data/ycfeng/Open-MOPD/evals/verifier/third_party/repos.lock.json'), PosixPath('/data/ycfeng/Open-MOPD/third_party/repos.lock.json'), PosixPath('/data/ycfeng/ModelMerging/third_party/repos.lock.json')]`. The --help case fails because line 103 runs load_repo_lock() before line 107 `args = parser.parse_args()`, so argparse never gets to handle --help. All three lock candidates confirmed absent on disk.",
    "correct_fact": "",
    "cluster": "evals"
  },
  {
    "claim_id": "C43",
    "claim": "/data/ycfeng/Open-MOPD/experiments/backend/README.md documents --base/--model/--output while param_merge.py's real flags are --mode/--input NAME=PATH/--reference/--output-dir.",
    "verdict": "IMPRECISE",
    "evidence": "/data/ycfeng/Open-MOPD/experiments/backend/README.md:10-14 `python -m experiments.backend.param_merge \\ --base /path/to/base \\ --model math=/path/to/math \\ --model code=/path/to/code \\ --output /path/to/merged`. /data/ycfeng/Open-MOPD/experiments/backend/param_merge.py:291-308: `--mode` (choices average|task-arithmetic, required=True), `--input` (action=append, type=_parse_named_path, required=True, metavar=\"NAME=PATH\"), `--reference` (type=Path, required=True), `--base` (type=Path, OPTIONAL \u2014 line 296), `--alpha` (default 1.0), `--alpha-for` (metavar NAME=ALPHA), `--output-dir` (type=Path, required=True), `--overwrite`.",
    "correct_fact": "Substantially right \u2014 the README command cannot run: `--model` and `--output` do not exist (real names are `--input NAME=PATH` and `--output-dir`), and the README omits the two required flags `--mode` and `--reference`. The detail that is off: `--base` IS a real flag (param_merge.py:296, optional `type=Path`, consumed at :325 `base_dir=args.base.resolve() if args.base is not None else None`), so it should not be listed among the README-only/nonexistent flags. Also unmentioned: real flags `--alpha`, `--alpha-for`, `--overwrite`.",
    "cluster": "evals"
  },
  {
    "claim_id": "C44",
    "claim": "mt_opd.sh and opd.sh emit actor_rollout_ref.rollout.reward_mode=<mode> WITHOUT a leading plus, reward_mode is declared in no yaml under trainer/config/ and absent from RolloutConfig; determine whether this is a Hydra composition error; check whether any script/test uses the plus form.",
    "verdict": "CONFIRMED",
    "evidence": "It is a hard Hydra composition error, now proven by execution (not inference).\n\n(a) The plain form, no plus:\n/data/ycfeng/Open-MOPD/scripts/local/mt_opd.sh:87 -> `    \"actor_rollout_ref.rollout.reward_mode=mt_opd\"`\n/data/ycfeng/Open-MOPD/scripts/local/opd.sh:66 -> `    \"actor_rollout_ref.rollout.reward_mode=${REWARD_MODE}\"` (REWARD_MODE default `opd_kl` at opd.sh:43)\n\n(b) Declared in no yaml, anywhere in the repo: `grep -rnE '^\\s*reward_mode\\s*:' --include=*.yaml --include=*.yml /data/ycfeng/Open-MOPD` -> 0 hits. /data/ycfeng/Open-MOPD/training/verl/verl/trainer/config/rollout/rollout.yaml (326 lines) starts `_target_: verl.workers.config.RolloutConfig` and has no reward_mode.\n\n(c) Absent from the dataclass: /data/ycfeng/Open-MOPD/training/verl/verl/workers/config/rollout.py:159 is only a comment -> `    # Reward-extrapolation strength for reward_mode=exopd. 1.0 reproduces plain`. No field.\n\n(d) No escape hatch: `grep -rn 'ConfigStore|cs.store' verl/` -> 0 hits (no structured-config schema merge). Only two set_struct calls exist and both ENABLE struct: ray_trainer.py:879 `OmegaConf.set_struct(self.config, True)` and fsdp_workers.py:879. Entry point is /data/ycfeng/Open-MOPD/training/verl/verl/trainer/main_ppo.py:36 -> `@hydra.main(config_path=\"config\", config_name=\"ppo_trainer\", version_base=None)` (line 36 exact).\n\n(e) EXECUTED PROOF. hydra-core 1.3.6 installed into an isolated target dir; probe at /data/ycfeng/tmp/hydra_probe/probe.py composed the real ppo_trainer config from /data/ycfeng/Open-MOPD/training/verl/verl/trainer/config:\n  [baseline] COMPOSED OK / rollout has reward_mode key: False\n  [plain reward_mode (as shipped)] RAISED ConfigCompositionException: Could not override 'actor_rollout_ref.rollout.reward_mode'.\n  To append to your config use +actor_rollout_ref.rollout.reward_mode=mt_opd\n  [+reward_mode] COMPOSED OK / reward_mode value: mt_opd\n  [+mt_opd.teacher_domains] COMPOSED OK\nSo both shipped launchers abort during Hydra composition, before Ray or any worker starts.\n\n(f) Intent: NO shell script or test anywhere in the repo passes reward_mode with a plus. `grep -rnE '\\+?actor_rollout_ref\\.rollout\\.reward_mode' /data/ycfeng/Open-MOPD` returns only mt_opd.sh:87, opd.sh:66 (both plain) plus task_memory prose. Intent is instead settled by the asymmetry inside the same file: mt_opd.sh:92-93 and :107-112 correctly prefix every other undeclared key (`+mt_opd.teacher_domains=`, `+mt_opd.n_additional_teachers=`, `+mt_reward_model_${i}.*`), and ray_trainer.py:86-91 reads reward_mode then deletes it under `open_dict` precisely because RolloutConfig rejects it. The missing `+` on this one key is an omission, and the fix is `+actor_rollout_ref.rollout.reward_mode=...`.",
    "correct_fact": "",
    "cluster": "launcher_defects"
  },
  {
    "claim_id": "C45",
    "claim": "No launcher sets actor_rollout_ref.rollout.log_prob_top_k, and ray_trainer.py:1968 reads it with default 0, so mt_opd/delta_opd raise at :1974.",
    "verdict": "CONFIRMED",
    "evidence": "Cited lines are exact.\n/data/ycfeng/Open-MOPD/training/verl/verl/trainer/ppo/ray_trainer.py:1968 -> `                            top_k = self.config.actor_rollout_ref.rollout.get(\"log_prob_top_k\", 0)`\n:1973 -> `                            if reward_mode in {\"delta_opd\", \"mt_opd\"} and top_k <= 0:`\n:1974 -> `                                raise ValueError(f\"reward_mode={reward_mode} requires actor_rollout_ref.rollout.log_prob_top_k > 0\")`\n\nNo launcher or yaml sets it. `grep -rn log_prob_top_k /data/ycfeng/Open-MOPD` (excluding task_memory) returns ONLY: two test docstrings (tests/test_hybrid_grpo_dense_topk.py:6, tests/test_exopd_trainer_wiring.py:8), ray_trainer.py:1968/1974/1981/2642, workers/config/rollout.py:154, workers/actor/dp_actor.py:612, workers/fsdp_workers.py:1022/2720. `grep -rn log_prob_top_k /data/ycfeng/Open-MOPD/scripts /data/ycfeng/Open-MOPD/experiments /data/ycfeng/Open-MOPD/training/scripts` -> 0 hits. `grep -rnE '^\\s*log_prob_top_k\\s*:' --include=*.yaml --include=*.yml` over the whole repo -> 0 hits.\n\nThe dataclass default is a decoy and does not apply here: workers/config/rollout.py:154 -> `    log_prob_top_k: int = 256`, but the trainer reads the raw Hydra DictConfig (ray_trainer.py:627 `self.config = config`; the `open_dict(rollout)` at :89 only works on a DictConfig), and the dataclass is applied later, worker-side, via omega_conf_to_dataclass.\n\nEXECUTED CONFIRMATION on the real composed config (probe /data/ycfeng/tmp/hydra_probe/probe.py):\n  rollout has log_prob_top_k key: False\n  rollout.get('log_prob_top_k', 0) -> 0\n\nOne ordering nuance worth carrying: because C44 aborts at Hydra composition, a user of the shipped mt_opd.sh never reaches :1974. The :1974 raise becomes the *second* failure, hit on the first training step once reward_mode is passed with a `+`. The raise sits under `if self.use_rm and \"rm_scores\" not in batch.batch.keys()` (:1957), and mt_opd forces use_rm=True (ray_trainer.py:658), so it is unavoidable on that path.",
    "correct_fact": "",
    "cluster": "launcher_defects"
  },
  {
    "claim_id": "C46",
    "claim": "mt_opd.sh sets model.input_tokenizer=null and model.use_remove_padding=True only for teachers 1..N-1 (the mt_reward_model_{i} block), never for teacher 0 (reward_model.model.*).",
    "verdict": "CONFIRMED",
    "evidence": "Teacher 0 block, /data/ycfeng/Open-MOPD/scripts/local/mt_opd.sh:90-91 (the only reward_model.* lines in the file):\n`    \"reward_model.enable=True\"`\n`    \"reward_model.model.path=${first_teacher}\"`\n\nTeachers i=1..N-1, mt_opd.sh:106-112 inside `for ((i = 1; i < ${#LOCAL_TEACHER_PATHS[@]}; i++))`:\n`        \"+mt_reward_model_${teacher_index}.enable=True\"`\n`        \"+mt_reward_model_${teacher_index}.model.path=${teacher_path}\"`\n`        \"+mt_reward_model_${teacher_index}.model.input_tokenizer=null\"`\n`        \"+mt_reward_model_${teacher_index}.model.use_remove_padding=True\"`\n`        \"+mt_reward_model_${teacher_index}.model.fsdp_config.param_offload=True\"`\n\nThe asymmetry is material because the overlay is merged onto the teacher-0 config: ray_trainer.py:1437 -> `                    mt_cfg = OmegaConf.merge(self.config.reward_model, extra_cfg_overlay)`. So teacher 0 keeps the yaml defaults while teachers 1..N-1 override them:\n/data/ycfeng/Open-MOPD/training/verl/verl/trainer/config/reward_model/reward_model.yaml:33 -> `  input_tokenizer: ${actor_rollout_ref.model.path}`\n/data/ycfeng/Open-MOPD/training/verl/verl/trainer/config/reward_model/dp_reward_model.yaml:24 -> `  use_remove_padding: False`\n\nOne addition the claim leaves out: `model.fsdp_config.param_offload=True` (mt_opd.sh:111) is likewise only set for i>=1, so teacher 0 also differs in memory placement.",
    "correct_fact": "",
    "cluster": "launcher_defects"
  },
  {
    "claim_id": "C47",
    "claim": "The shipped mt_opd.sh sets none of mt_opd.target_gradient_shares / target_share_* / normalize_reward_scale / conflict_policy / domain_weighting, so as released it produces naive M-OPD (routing only) with compute_domain_loss_weights returning None.",
    "verdict": "CONFIRMED",
    "evidence": "Read /data/ycfeng/Open-MOPD/scripts/local/mt_opd.sh in full (122 lines). The only mt_opd.* keys it emits are :92 `\"+mt_opd.teacher_domains=${domain_list}\"` and :93 `\"+mt_opd.n_additional_teachers=$((teacher_count - 1))\"`. Repo-wide grep for target_gradient_shares|target_share|normalize_reward_scale|conflict_policy|domain_weighting over --include=*.sh --include=*.yaml --include=*.yml --include=*.md, excluding task_memory -> 0 hits. Only three .py files mention them: verl/trainer/ppo/ray_trainer.py, verl/workers/actor/mt_opd.py (the implementation), tests/test_mt_opd_m2_m3.py.\n\nResulting defaults in ray_trainer.py (all read via OmegaConf.select on an absent `mt_opd` subtree):\n:664 -> `            self.mt_domain_weighting: str = str(OmegaConf.select(mt_opd_cfg, \"domain_weighting\") or \"domain_routing\")`\n:674 -> `            self.mt_target_gradient_shares: dict[str, float] | None = None` (and neither the target_share_domains/target_share_values branch at :675-687 nor the target_gradient_shares branch at :690-694 fires)\n:706-708 -> `            _rs = OmegaConf.select(mt_opd_cfg, \"normalize_reward_scale\")` / `            if _rs is None or _rs is False:` / `                self.mt_normalize_reward_scale: bool | float = False`\n:746-748 -> `            self.mt_conflict_policy: str = str(` / `                OmegaConf.select(mt_opd_cfg, \"conflict_policy\") or \"none\"` / `            )`\n\nAnd the loss weighting is then a no-op: call site ray_trainer.py:2202-2205 passes `target_shares=self.mt_target_gradient_shares`, and /data/ycfeng/Open-MOPD/training/verl/verl/workers/actor/mt_opd.py:207-208 -> `    if target_shares is None or domains is None or len(domains) == 0:` / `        return None`. Its own docstring at mt_opd.py:189-190 states `None` means \"leave the loss alone\". So the released launcher yields routing-only M-OPD: domain_weighting=domain_routing, no gradient-share correction, no reward-scale normalization, conflict_policy=none.",
    "correct_fact": "",
    "cluster": "launcher_defects"
  },
  {
    "claim_id": "C48",
    "claim": "install_requirements.sh installs neither torch, nor vllm, nor flash-attn, while all three PPO launchers hard-code rollout.name=vllm. Also verl's setup.py/requirements pin numpy<2.0.0.",
    "verdict": "IMPRECISE",
    "evidence": "vllm / flash-attn / numpy / launchers: all CONFIRMED.\n/data/ycfeng/Open-MOPD/training/install_requirements.sh:10 -> `\"${PIP_BIN[@]}\" install --user swanlab math-verify matplotlib duckdb`; :13 -> `\"${PIP_BIN[@]}\" install --user -e .` (no extras marker); :16-18 -> ray==2.55.1, transformers==4.57.6, protobuf==3.20.3; :21 -> requirements-if-rl.txt (nltk/langdetect/absl-py/immutabledict only). No torch, vllm, or flash-attn install line anywhere in the file.\nverl/setup.py keeps both behind unused extras: :50 -> `GPU_REQUIRES = [\"liger-kernel\", \"flash-attn\"]`; :52 -> `VLLM_REQUIRES = [\"tensordict>=0.8.0,<=0.10.0,!=0.9.0\", \"vllm>=0.8.5,<=0.11.0\"]`. Neither is in install_requires (:26-45). verl/requirements.txt:19 even has it commented out -> `# vllm==0.8.4`.\nHard-coded vllm in all three PPO launchers: mt_opd.sh:86, opd.sh:65, rl.sh:55, each `\"actor_rollout_ref.rollout.name=vllm\"`.\nnumpy pin: setup.py:32 -> `    \"numpy<2.0.0\",` and requirements.txt:8 -> `numpy<2.0.0`.\n\nThe torch half is the imprecise part: `pip install -e .` does end up installing torch, transitively.",
    "correct_fact": "install_requirements.sh installs no vllm and no flash-attn (both are extras-only, never requested), and it never installs torch EXPLICITLY or pins its version/CUDA build - but torch is still pulled in transitively by `pip install --user -e .`, because install_requires lists `accelerate` (setup.py:27), `peft` (:34), `torchdata` (:39) and `tensordict>=0.8.0,<=0.10.0,!=0.9.0` (:40), each of which depends on torch. The practical defect is therefore \"unpinned/default-index torch plus zero rollout backend\" rather than \"no torch at all\".",
    "cluster": "launcher_defects"
  }
]